#include <bits/stdc++.h>
using namespace std;

char A[1002], B[1002];
int DP[1002];

int ans, len_a, len_b;

int main()
{
    scanf("%s %s", A, B);
    len_a = strlen(A);
    len_b = strlen(B);
    for (int i = 0; i < len_a; i++)
    {
        int idx_a =
    }
}