/* Code By [ junah ]
GitHub : [ junah201 ] */

#include <bits/stdc++.h>
using namespace std;

int N, M;
vector<int> A;

int main()
{
    scanf("%d", &N);
    A.resize(N);

    for (int i = 0; i < N; i++)
    {
        scanf("%d", &A[i]);
    }

    scanf("%d", &M);

    return 0;
}