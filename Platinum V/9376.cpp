#include <bits/stdc++.h>
using namespace std;

int solve()
{
    int H, W;
    scanf("%d %d", &H, &W);
    char inp[102][102];
    for (int y = 0; y < H; y++)
        scanf("%s", inp[y]);
}

int main()
{
    int T;
    scanf("%d", &T);
    while (T--)
    {
        printf("%d\n", solve());
    }
}