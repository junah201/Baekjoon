#include <bits/stdc++.h>
using namespace std;

int N, K, ans;

priority_queue<int, vector<int>, greater<int>> high;
priority_queue<int, vector<int>> low;

int main()
{
    scanf("%d %d", &N, &K);
    for (int i = 0, x; i < N; i++)
    {
        scanf("%d", &x);
    }
}