//1920  수 찾기

#include <stdio.h>
#include <algorithm>

using namespace std;

int main() {
    int N; scanf("%d",&N);
    int check[100000],input[100000];
    for(int i=0;i<N;i++) scanf("%d",&check[i]);
    //sort(check,check+N);
    int M; scanf("%d",&M);
    bool count=false;
    for(int i=0;i<M;i++) sc
    return 0;
}